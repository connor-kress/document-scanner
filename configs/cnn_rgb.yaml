name: cnn_rgb
model_type: cnn
color_mode: rgb
scheme: split_video
image_size: [384, 216]
batch_size: 32
learning_rate: 0.0003
weight_decay: 0.0001
dropout: 0.3
loss: smooth_l1
max_epochs: 60
patience: 12
random_seed: 42
train_fraction: 1.0
subset_seed: 42
